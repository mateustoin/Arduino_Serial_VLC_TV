#include "GerenciaCanal.h"

GerenciaCanal::GerenciaCanal(){
	/*arduino = ArduinoSerial("/dev/ttyACM0");*/
}

void GerenciaCanal::iniciaPlaylist(){
	/*int comando;

	while (0){
		comando = arduino.recebeSinali();
		if (comando == 9){
			player.Play();
		}

		if (comando == 10){
			player.Stop();
		}
	}

	while (1){
		//comando = arduino.recebeSinali();
		player.Playlist(arduino);
	}*/
}